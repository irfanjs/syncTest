<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form" xmlns:s="/lib/samples">
<f:entry>
<div name="parameter">
        <input type="hidden" name="name" value="${it.name}" />
        <!--<f:dropdownDescriptorSelector title="Sync Option" field="option" descriptors="${it.optionDescriptors}" default="${descriptor.displayName.equals(it.defaultValue)}"/> -->
        <f:dropdownDescriptorSelector title="Sync Option" field="option" descriptors="${it.optionDescriptors}" default="Normal"/>
</div>
</f:entry>
</j:jelly>